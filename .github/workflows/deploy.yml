/*
name: Construccion infraestructura en Azure
on: [push]
jobs:
  Deploy-infra:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
*/

# tfsec:ignore:azure-resource-group-no-vulnerabilities
resource "azurerm_resource_group" "rg" {
  name     = "upt-arg-ok"
  location = "eastus"
}